---
 - hosts: localhost
   remote_user: root
   become: yes
   become_user: root
   vars:
     ansible_become_pass: "kovair@123"
     ansible_ssh_pass: "kov$2010$air"
   tasks:
    - name: Install gimp
      yum: name=gimp state=installed
