---
 - hosts: all
   user: root
   tasks:
    - name: Install Jetty
      apt: pkg=jetty state=installed
    - name: Copies a file from control machine to all hosts
      copy: src=/var/tmp/roles/text.txt dest=/tmp/
